# decisions/ — empty by inheritance

The skill's checkpoint protocol expects confirmed `CP-*.yaml` records here, but
the worked example predates the protocol: its CP-0/2/3/4/5 decisions were made
in chat and survive only implicitly (shipped copy, RETROSPECTIVE §5, the golden
values in `config.yaml`). Recorded as DOC_GAPS.md #7.

Do not backfill records nobody confirmed; new races get real ones.
